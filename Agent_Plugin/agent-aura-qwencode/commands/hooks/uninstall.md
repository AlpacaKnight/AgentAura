# Uninstall AgentAura Hooks

移除 `agent-aura-qwencode` 写入的 Qwen Code hooks。

执行：

1. 确认 `agent-aura-qwencode` 命令已安装
2. `agent-aura-qwencode uninstall-hooks`
3. `agent-aura-qwencode hooks print`
4. 完全退出并重新启动 Qwen Code

说明：只删除 `agent-aura-qwencode:` 前缀的 hook 项，不影响其他 Qwen 配置。
