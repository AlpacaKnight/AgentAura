# Install AgentAura Hooks

为 Qwen Code 安装 AgentAura hooks。

执行：

1. 确认 `agent-aura-qwencode` 命令已安装
2. `agent-aura-qwencode install-hooks`
3. `agent-aura-qwencode hooks print`
4. 完全退出并重新启动 Qwen Code

安装完成后，向用户说明 hooks 已写入 `~/.qwen/settings.json`。
