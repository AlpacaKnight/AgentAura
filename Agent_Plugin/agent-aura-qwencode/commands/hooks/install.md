# Install AgentAura Hooks

为 Qwen Code 安装 AgentAura hooks。

执行：

1. `cd Agent_Plugin/agent-aura-qwencode`
2. `npm install`
3. `npm run compile`
4. `node out/index.js install-hooks`
5. `node out/index.js hooks print`

安装完成后，向用户说明 hooks 已写入 `~/.qwen/settings.json`。
