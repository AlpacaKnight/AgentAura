{
  "name": "agent-aura-claude",
  "displayName": "AgentAura - Claude Ring Light",
  "description": "Sync Claude Code lifecycle events to AgentAura ESP32 Ring Light hardware",
  "version": "0.1.0",
  "author": "AgentAura",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "agent-aura-claude": "./bin/agent-aura-claude.js"
  },
  "files": [
    ".claude-plugin/**",
    "agent-aura-claude.config.example.json",
    "bin/**",
    "commands/**",
    "hooks/**",
    "src/**",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "build": "npm run lint && npm run test:unit",
    "lint": "node --check bin/agent-aura-claude.js && node --check src/config.js && node --check src/deviceClient.js && node --check src/discovery.js && node --check src/hooks.js && node --check src/index.js && node --check src/types.js && node --check test/regression.test.js",
    "test": "npm run test:unit",
    "test:unit": "node --test test/*.test.js"
  },
  "keywords": [
    "claude-code",
    "agentaura",
    "esp32",
    "ring-light",
    "hooks"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "serialport": "^12.0.0"
  }
}
