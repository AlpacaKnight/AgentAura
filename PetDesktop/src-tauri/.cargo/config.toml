# Force system GCC instead of conda's GCC to avoid sysroot mismatches
# (conda's Scrt1.o references __libc_csu_init/__libc_csu_fini which don't exist)
[target.x86_64-unknown-linux-gnu]
linker = "/usr/bin/gcc"
